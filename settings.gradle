pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'DevLib'

include(':api')
project(':api').projectDir = file('api')

include(':plugin')
project(':plugin').projectDir = file('plugin')